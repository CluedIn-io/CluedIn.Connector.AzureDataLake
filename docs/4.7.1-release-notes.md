### Features
- Add Amazon S3 connector for publishing data to Amazon S3 buckets
- Extract storage-agnostic `IExternalFileStorageClient` interface from Azure-specific `IDataLakeClient` to support non-Azure storage backends
- Introduce `IStorageDirectoryClient`, `IStorageFileClient`, and `IStorageFileProperties` abstractions in the common project

### Fixes
