### Features
- Add Amazon S3 connector
- Add additional connector actions (Get Connector version, Allow retrieval of buffer status)
- Add support for multiple buffer strategies
- Add ability to prevent concurrent builds
- Prevent race condition causing inconsistent state
- Add launcher for local development

### Improvements
- Refactor to File Storage common layer, extracting shared code across connectors
- Update to .NET 10

### Fixes
- Fixed missing registration by always registering and using lock to prevent duplicate registration
- Fixed devonly project file to match nuget packages
- Downgraded package to match Core
